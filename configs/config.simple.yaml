providers:
  openai:
    api_key: "${OPENAI_API_KEY}"

routing:
  routes:
    - name: "default"
      targets:
        - provider: openai
          model: gpt-5.2

# Optional: enable inbound API-key auth for /v1/*
# security:
#   enabled: true
#   provider: "api_key"
#   api_key:
#     keys:
#       - value: "${LUNARGATE_CLIENT_API_KEY}"

# Optional: connect to LunarGate Dashboard observability
# data_sharing:
#   enabled: true
#   share_prompts: true
#   share_responses: true
#   # Create this key in the "Gateways" section of https://app.lunargate.ai
#   api_key: "${LUNARGATE_GATEWAY_API_KEY}"
