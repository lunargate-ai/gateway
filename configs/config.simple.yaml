providers:
  openai:
    api_key: "${OPENAI_API_KEY}"

routing:
  routes:
    - name: "default"
      targets:
        - provider: openai
          model: gpt-5.2

# Optional: enable inbound API-key auth for /v1/*
# security:
#   enabled: true
#   provider: "api_key"
#   api_key:
#     keys:
#       - value: "${LUNARGATE_CLIENT_API_KEY}"

# Optional: shared gateway identity for Dashboard integrations
# general:
#   api_key: "${LUNARGATE_GATEWAY_API_KEY}"
#   backend_url: "${LUNARGATE_BACKEND_URL}"

# Optional: connect to LunarGate Dashboard observability
# data_sharing:
#   enabled: true
#   share_prompts: true
#   share_responses: true
